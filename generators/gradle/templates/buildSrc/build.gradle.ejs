plugins {
    id 'groovy-gradle-plugin'
}

repositories {
    gradlePluginPortal()
}

dependencies {
    // avan-needle-gradle-dependency - JHipster will add additional dependencies for convention plugins here
    // avan-needle-gradle-build-src-dependency - Deprecated: JHipster will add additional dependencies for convention plugins here
}
